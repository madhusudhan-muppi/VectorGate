This directory contains the generated DEMONSTRATION / SYNTHETIC CLASSIFICATION MODEL artifacts.

Generate them with:

```powershell
python -m software.classifier.train_demo
```

They are not trained on biological mosquito data and must not be described as field or species-classification performance.
